{
  "name": "zoho-books",
  "version": "1.0.0",
  "description": "Browser automation agent that collects invoices with status **Paid** or **Partially Paid** from the Zoho Books Demo Company.   The script navigates the demo site (without API), scrapes invoices across all pages, and outputs a CSV file with the required columns.",
  "main": "index.js",
  "scripts": {
    "scrape": "node scripts/scrape.js",
    "scrape:headful": "HEADFUL=true node scripts/scrape.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
